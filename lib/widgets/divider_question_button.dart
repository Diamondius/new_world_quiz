import 'package:flutter/material.dart';

class DividerQuestionButton extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Divider(
      color: Theme.of(context).primaryColorDark,
      thickness: 5,
      height: 2,
    );
  }
}
